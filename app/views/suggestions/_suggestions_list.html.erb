<div class= "my-3" style="width: 20rem;">
    <div class="d-flex flex-row justify-content-between">
        <p class="text-muted"> Рекомендації для вас </p>
        <%= link_to root_path, style:"text-decoration: none;" do %>
            <small class="fw-bold text-dark">Побачити всі</small>
        <% end %>
    </div>

    <div class="d-flex flex-column gap-3 px-2">
        <% (0..5).each do %>
            <%= render "suggestions/suggestion" %>  
        <% end %>
    </div>
</div>